import React from 'react';

const Header = (props) =>(

    //Page header
    <div className="header">

        <h1>List of 10,000 numbers in random order</h1>
        <h5>Each number in the list will be unique, between 1 and 10,000</h5>
        

    </div>

);

//export component
export {Header as default}
